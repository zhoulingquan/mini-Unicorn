# 心跳任务

本文件会被你的 miniUnicorn agent 定期检查。
将其注册为 cron 任务（例如 `cron add --name heartbeat --schedule "every 30m" --message "Check HEARTBEAT.md"`），即可获得与旧版心跳服务相同的行为。

如果本文件没有任何任务（只有标题和注释），agent 会跳过它。

## 进行中的任务

<!-- 在此行下方添加你的周期性任务 -->


## 已完成

<!-- 将已完成任务移到此处，或直接删除 -->
